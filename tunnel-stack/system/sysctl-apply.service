[Unit]
Description=Apply tunnel-stack sysctl hardening
DefaultDependencies=no
After=local-fs.target
Before=network-pre.target

[Service]
Type=oneshot
ExecStart=/usr/sbin/sysctl --system

[Install]
WantedBy=multi-user.target
